# Pinned exactly, not ranged: "same bytes in, same text out" has to hold across time, and
# a floating PyMuPDF is a floating text layer. The old ranges were already lying — the
# runner resolved py7zr<1 while the desktop that validated the corpus ran 1.1.3. These are
# the versions the acceptance numbers were measured with; bump deliberately, re-running
# the acceptance tender.
PyMuPDF==1.28.0
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
py7zr==1.1.3
